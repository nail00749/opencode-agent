{
  "$schema": "./schema.json",
  // Without external trust, only $schema and descriptions of existing agents
  // are allowed. All other project overrides require the exact token printed by
  // `gvozd trust-project` after review; this file cannot authorize itself.
  "agents": {}
}
